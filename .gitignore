# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/
# Note: uv.lock and .python-version ARE tracked — they pin the toolchain.

# Local model / audio caches (Jarvis sensing + ML)
models/
*.gguf
*.bin
audio_cache/

# Captured conversations (ephemeral by default — never commit)
captures/
