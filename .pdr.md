---
schema_version: 0.4
project_name: project-jarvis
bootstrap_mode: greenfield
detected_framework: none
detected_router: n/a
detected_language: python
detected_styling: []
detected_ui_library: none
detected_db_layer: none
detected_auth: none
detected_existing_docs:
  - docs/prd/README.md
  - docs/prd/01-conversation-initiation.md
  - docs/prd/02-jarvis-v0-mvp.md
  - prototypes/attention-layer/README.md
detected_existing_agents: false
monorepo_layout: none
surfaces: [cli]
domain_entities:
  - name: utterance
    description: a transcribed chunk of speech
    examples: [speaker, text, timestamp]
  - name: rolling-window
    description: bounded sliding buffer of the most recent utterances
    examples: [max_utterances, max_seconds]
  - name: living-summary
    description: continuously maintained, delta-updated summary of the current conversation
    examples: [text, basis_keywords]
  - name: wall
    description: a moment the conversation could use help
    examples: [category, confidence, offer]
  - name: engagement-handoff
    description: the context package passed downstream when Jarvis engages
    examples: [trigger_reason, summary, recent_excerpt]
  - name: interjection
    description: a proactive offer to help on a detected wall
    examples: [category, offer, confidence]
pii_entities: [utterance, rolling-window, living-summary]
phases:
  - id: phase_0
    name: Foundations
    goal: Python scaffold, the six deep core modules with unit tests, and an end-to-end MOCK pipeline running green.
  - id: phase_1
    name: Real ears
    goal: Always-on mic + Silero VAD + local ASR producing a live transcript on the M5 (ASR runtime selected via spike).
  - id: phase_2
    name: Local understanding
    goal: Living Summary and Wall Detection backed by a local Qwen2.5 (MLX) model, replacing the mock backend.
  - id: phase_3
    name: Knowing when to speak
    goal: TurnTakingGate + SummonController wired to real VAD timing — fast wake-word summon, conservative polite interjection, abort-on-resume.
  - id: phase_4
    name: The voice
    goal: Engaged path — Claude composes a spoken-style grounded answer streamed into ElevenLabs voice output.
  - id: phase_5
    name: Make it live & tune
    goal: Full always-on loop on the M5; tune interjection thresholds against the precision metric on captured conversations.
approved_stack:
  - tool: Python 3.11+
    role: orchestration language for the whole pipeline
    alternatives_to_propose_before_swap: [Rust, C++]
  - tool: Anthropic Claude API (claude-opus-4-8)
    role: engaged-path answer generation (spoken-style, grounded in context)
    alternatives_to_propose_before_swap: [local large model]
  - tool: ElevenLabs
    role: streamed text-to-speech voice output
    alternatives_to_propose_before_swap: [local TTS (Piper)]
  - tool: Silero VAD
    role: voice-activity detection / silence gating
    alternatives_to_propose_before_swap: [WebRTC VAD]
  - tool: Local ASR (mlx-whisper or whisper.cpp — selected in Phase 1 spike)
    role: on-device speech-to-text
    alternatives_to_propose_before_swap: [faster-whisper, Moonshine, Parakeet]
  - tool: Qwen2.5 via MLX (size selected in Phase 2 spike)
    role: local ambient reasoning — living summary + wall detection
    alternatives_to_propose_before_swap: [Llama, Phi, other small local models]
compliance_regime: [none]
hard_nos:
  - Never send ambient audio or transcripts to the cloud during listening — the cloud is touched only at the moment of answering.
  - Never persist the transcript by default — it is ephemeral; retention only on explicit opt-in.
  - Never interject mid-sentence or below the confidence threshold; always abort an interjection if speech resumes.
  - Never let v0 drift into out-of-scope items (phone port, off-grid, wearables, fine-tuning, diarization, full-duplex, local TTS, local-query routing, cross-session memory).
agent_roster:
  - name: core-engineer
    description: Use for the pure-logic attention core — the rolling window, living-summary delta-update, wall detection, the turn-taking gate, the dual-summon state machine, and the mock orchestrator. Trigger whenever a Phase 0 or Phase 3 task touches module logic or the summon/timing behavior.
    model: sonnet
    rationale: Phases 0 and 3 are dominated by deterministic, pure-logic modules and state machines — the heart of the MVP and the bulk of the test surface.
    scope_owns:
      - RollingWindow, TopicShiftDetector, LivingSummary, WallDetector (interface + mock backend)
      - TurnTakingGate and SummonController state machines
      - AttentionLayer orchestrator and the ScriptedSource end-to-end mock pipeline
      - Module interfaces and boundaries (the seams the I/O adapters plug into)
    scope_does_not:
      - Real audio capture / VAD / ASR integration (sensing-engineer)
      - Local model inference and prompting (local-ml-engineer)
      - Cloud answer + voice integration (voice-integration-engineer)
    domain_folder: docs/architecture/
    is_mandatory_reviewer: false
    mandatory_review_triggers: null
    initial_handoffs_to:
      - agent_name: voice-integration-engineer
        condition: an EngagementHandoff is produced and needs an engaged response
      - agent_name: qa-tuning
        condition: a core module is ready for tests or its behavior changes
      - agent_name: human
        condition: an architectural decision exceeds the PRD's settled scope
    first_task_deliverable:
      file: docs/architecture/module-map.md
      description: the module boundaries, interfaces, and event flow for the attention layer
  - name: sensing-engineer
    description: Use for the local always-on audio path — microphone capture, Silero VAD gating, ASR integration, and the MicSource TranscriptSource adapter. Trigger on Phase 1 tasks and the ASR half of the runtime spike.
    model: sonnet
    rationale: Phase 1 is a distinct real-time audio discipline (capture loop, buffering, VAD gating, latency) separate from both pure logic and LLM inference.
    scope_owns:
      - Always-on microphone capture loop and buffering
      - Silero VAD integration and speech/silence segmentation
      - Local ASR integration and the ASR half of the Phase 1 runtime spike
      - The MicSource TranscriptSource adapter feeding Utterance events
    scope_does_not:
      - Core attention logic and state machines (core-engineer)
      - SLM summary/wall inference (local-ml-engineer)
      - Cloud voice output (voice-integration-engineer)
    domain_folder: docs/audio/
    is_mandatory_reviewer: false
    mandatory_review_triggers: null
    initial_handoffs_to:
      - agent_name: core-engineer
        condition: Utterance events and VAD timing are available to feed the window and the gate
      - agent_name: local-ml-engineer
        condition: jointly running the Phase 1 runtime spike (ASR + SLM on the same M5 budget)
      - agent_name: human
        condition: hardware/runtime constraints force a scope or stack change
    first_task_deliverable:
      file: docs/audio/asr-spike.md
      description: the plan and results for the mlx-whisper vs whisper.cpp ASR runtime spike
  - name: local-ml-engineer
    description: Use for local on-device reasoning — running Qwen2.5 via MLX and implementing the summarize() and detect_wall() backends with prompting and structured output. Trigger on Phase 2 tasks and the SLM half of the runtime spike.
    model: sonnet
    rationale: Phase 2 is local LLM/SLM inference and prompt engineering on MLX — a distinct skill from real-time audio and from the pure-logic core.
    scope_owns:
      - Qwen2.5 (MLX) runtime integration and model-size selection
      - The local summarizer backend (LivingSummary's summarize implementation)
      - The local wall-detection backend (structured-output verdicts)
      - Replacing the mock backend behind the existing interfaces without breaking core tests
    scope_does_not:
      - Core module interfaces and state machines (core-engineer)
      - Audio capture and ASR (sensing-engineer)
      - Cloud answer generation (voice-integration-engineer)
    domain_folder: docs/ml/
    is_mandatory_reviewer: false
    mandatory_review_triggers: null
    initial_handoffs_to:
      - agent_name: core-engineer
        condition: the local backend is ready to plug into LivingSummary/WallDetector
      - agent_name: qa-tuning
        condition: wall-detection behavior changes and needs precision evaluation
      - agent_name: human
        condition: model quality is insufficient and a stack change is warranted
    first_task_deliverable:
      file: docs/ml/slm-backend.md
      description: the SLM runtime choice, prompt designs, and the summarize/detect_wall contracts
  - name: voice-integration-engineer
    description: Use for the engaged path — composing Claude's spoken-style, context-grounded answer and streaming it through ElevenLabs to the speaker. Trigger on Phase 4 tasks and anything touching the response-style contract or engaged-path latency.
    model: sonnet
    rationale: Phase 4 is cloud-service integration (Claude + ElevenLabs) plus the spoken-response-style contract and streaming latency — its own integration lane.
    scope_owns:
      - EngagedResponder — Claude spoken-style answer grounded in the handoff
      - VoiceOutput — ElevenLabs streamed TTS
      - Token-stream Claude into ElevenLabs for first-audio latency
      - The spoken response-style contract (brevity, no preamble, plain prose)
    scope_does_not:
      - Ambient/local pipeline (core, sensing, local-ml engineers)
      - When-to-speak timing (core-engineer / qa-tuning)
    domain_folder: docs/voice/
    is_mandatory_reviewer: false
    mandatory_review_triggers: null
    initial_handoffs_to:
      - agent_name: core-engineer
        condition: the EngagementHandoff shape needs adjustment to carry enough context
      - agent_name: human
        condition: API keys, costs, or voice choice need a product decision
    first_task_deliverable:
      file: docs/voice/response-contract.md
      description: the spoken response-style prompt and the Claude-to-ElevenLabs streaming design
  - name: qa-tuning
    description: Use for the test strategy and the success-metric work — unit tests for the six core modules (simulated clock + fakes), the interjection-precision eval, and threshold calibration. MANDATORY reviewer for any change to the wall/summon/timing behavior. Trigger on Phase 0 test tasks, Phase 5 tuning, and any change to TurnTakingGate/SummonController/WallDetector.
    model: opus
    rationale: Interjection precision is the project's success metric. This agent defines how it is measured, calibrates the thresholds, and independently reviews the riskiest behavior — decision-shaping work that warrants Opus.
    scope_owns:
      - Test strategy and the reusable test harness (simulated clock, fakes)
      - Unit tests for the six core modules (external-behavior tests)
      - The interjection-precision eval definition and fixtures
      - Threshold/back-off calibration on captured conversations (Phase 5)
    scope_does_not:
      - Owning feature implementation (the engineer agents own their lanes)
      - Audio/model/voice runtime choices (those agents decide; qa reviews behavior)
    domain_folder: docs/qa/
    is_mandatory_reviewer: true
    mandatory_review_triggers:
      - changes to TurnTakingGate
      - changes to SummonController
      - changes to WallDetector or its thresholds
      - changes to the interjection confidence / politeness-gap defaults
    initial_handoffs_to:
      - agent_name: core-engineer
        condition: a test reveals a behavior bug in a core module
      - agent_name: local-ml-engineer
        condition: wall-detection precision is below target and the backend needs work
      - agent_name: human
        condition: the success metric cannot be met within scope and a re-scope is needed
    first_task_deliverable:
      file: docs/qa/eval-plan.md
      description: the test conventions (simulated clock + fakes) and the interjection-precision eval spec
---

# project-jarvis

## One-line pitch
A local, always-on desktop assistant that follows your conversation and answers when summoned — or offers help when it hears an unanswered question — speaking back in a natural voice.

## The wedge
A single power user goes from *summon-and-re-explain* to *Jarvis already has the context and offers help within ~2 seconds of an unanswered question* — with no ambient audio ever leaving the device.

## Target user
Me — a technical builder who wants a present, Jarvis-like assistant on my own desktop and is comfortable running local models. Single user (the developer) for v0.

## Methodology
Graduated attention beyond the binary wake word, combined with turn-taking / endpointing theory: transition-relevance places, VAD + semantic-completeness endpointing, and an asymmetric summon-vs-interjection timing model. The conversation understanding is a delta-updated living summary that "redraws only the changed pixels."

(Source: Conversation Analysis turn-taking — Sacks/Schegloff/Jefferson; LiveKit end-of-turn detection; RESPOND, arXiv 2603.21682; full-duplex survey, arXiv 2509.14515; internal PRD 01 & 02.)

## Voice register
Spoken and conversational — like a competent peer who was listening. 1–3 sentences, no preamble, no "According to…", never encyclopedic/wiki, plain prose (it is spoken aloud).

## Success metric
Interjection precision: of the times Jarvis proactively offers help, the share that land at a genuinely useful, well-timed moment is high enough to keep the feature enabled (starting target ≥70% useful, with false interjections rare — to be calibrated on captured conversations).
